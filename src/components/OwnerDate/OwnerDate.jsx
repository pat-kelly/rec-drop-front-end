const OwnerDate = ({rec}) => {
  console.log(rec)
  return ( 
    <>  
      {/* <p>Owner: {rec.owner.name}</p> */}
      {/* <p>Date: {rec.createdAt.toISOString().slice(0, 16)}</p> */}
    </>
  );
}

export default OwnerDate;